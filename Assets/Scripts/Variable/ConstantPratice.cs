using UnityEngine;

public class ConstantPratice : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int first;
        int second;
        int third;

        first = 10;
        second = 20;
        third = 30;

        Debug.Log(first + "," + second + "," + third);
    }
}
